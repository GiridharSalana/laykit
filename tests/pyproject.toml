[project]
name = "laykit-tests"
version = "0.0.0"
description = "Test dependencies for LayKit validation"
requires-python = ">=3.10"
dependencies = [
    "gdstk>=0.9.0",
]

[dependency-groups]
dev = []

